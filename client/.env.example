# Copy this file to .env and adjust if your API runs elsewhere.
# Only variables prefixed with VITE_ are exposed to the browser.

# Base URL of the TokTickIT API.
VITE_API_URL="http://localhost:3001"
