import { Card } from '../components/Card'

/**
 * Create Ticket stub (ui-spec.md §6.2). The form, validation, attachment
 * picker, and submit/success/failure states land in a later Lab 2 issue.
 */
export function CreateTicketPage() {
  return (
    <div className="ttk-page-stub">
      <Card>
        <h2>Create Ticket</h2>
        <p>This screen arrives in a later Lab 2 issue.</p>
      </Card>
    </div>
  )
}
