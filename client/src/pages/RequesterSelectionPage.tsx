import { Card } from '../components/Card'

/**
 * Development Requester Selection stub (ui-spec.md §6.1). The real selector
 * — dropdown of active Requesters, loading/empty/failure states, and the
 * Requester-selection context that the app shell reads from — lands in
 * Issue #14.
 */
export function RequesterSelectionPage() {
  return (
    <div className="ttk-page-stub">
      <Card>
        <h2>Development Requester Selection</h2>
        <p>This screen arrives in a later Lab 2 issue.</p>
      </Card>
    </div>
  )
}
