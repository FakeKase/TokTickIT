{
  "name": "toktickit",
  "private": true,
  "description": "Workspace root: holds only the Playwright suite, which spans client and server",
  "scripts": {
    "e2e": "playwright test",
    "e2e:report": "playwright show-report",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^26.4.1",
    "typescript": "^7.0.2"
  }
}
